import {async, ComponentFixture, TestBed} from "@angular/core/testing";
import {StimItemComponent} from "./stim-item.component";
import {Logger} from "../../../../../core/logger.service/logger.service";
import {AppModule} from "../../../../../root/app.module";
import {APP_BASE_HREF} from "@angular/common";

describe('StimItemComponent', () => {
  // TODO: Uncomment test and resolve "Cannot read property 'content' of undefined error
  // let component: StimItemComponent;
  // let fixture: ComponentFixture<StimItemComponent>;
  //
  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [
  //       AppModule
  //     ],
  //     declarations: [],
  //     providers: [
  //       Logger,
  //       {provide: APP_BASE_HREF, useValue: '/'}
  //     ]
  //   }).compileComponents();
  // }));
  //
  // beforeEach(() => {
  //   fixture = TestBed.createComponent(StimItemComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
  //
  // it('should be created', () => {
  //   expect(component).toBeTruthy();
  // });
});
